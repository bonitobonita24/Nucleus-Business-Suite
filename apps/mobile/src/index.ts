// ─────────────────────────────────────────────────────────────────────────────
// Nucleus Mobile — Entry Point
// Framework: Expo (managed workflow) — iOS + Android
// Distribution: Internal (MDM / direct install) — NOT App Store
// Offline-first: WatermelonDB, auto-sync on reconnect
// Auth: Auth.js session via JWT Authorization header (no CSRF)
// ─────────────────────────────────────────────────────────────────────────────

export {};
